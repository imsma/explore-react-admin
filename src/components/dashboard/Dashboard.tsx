import { Card, CardContent, CardHeader } from "@mui/material";

const Dashboard = () => {
  return (
    <Card>
      <CardHeader title="The Dashboard" />
      <CardContent>This is sampel card</CardContent>
    </Card>
  );
};

export default Dashboard;
